[package]
name = "morningstar"
version = "0.1.0"
authors = ["Eric Stokes"]

[dependencies]
error-chain="0.12"
libmodbus-rs="0.8"
bitflags="1.0"
half = "1.3"
uom = { version = "0.20", features = ["use_serde", "f32", "f64", "si", "std"] }
chrono = { version = "0.4", features = ["serde"] }
serde = "1.0"
serde_derive = "1.0"
